# Copy to `dashboard/.streamlit/secrets.toml` (gitignored) or edit the repo’s
# default `secrets.toml` (local dev password: local-dev).
# Streamlit Cloud: set secrets in the app settings UI instead.

password = "change-me-before-deploying"

# Large corpus CSV (required in cloud if not committed as dashboard/data/merged_analysis.csv):
# Use an https:// URL to the file, or a path relative to the repo root on the server.
# DASHBOARD_CSV = "https://example.com/merged_analysis.csv"
