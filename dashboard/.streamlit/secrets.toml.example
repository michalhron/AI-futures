# Copy to `dashboard/.streamlit/secrets.toml` (gitignored) or edit the repo’s
# default `secrets.toml` (local dev password: local-dev).
# Streamlit Cloud: set secrets in the app settings UI instead.

password = "change-me-before-deploying"
